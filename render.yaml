services:
  # Signaling server
  - type: web
    name: freevoice-signaling
    env: node
    buildCommand: npm install
    startCommand: node signaling-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8888
    healthCheckPath: /
